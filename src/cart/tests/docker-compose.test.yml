services:
  cart:
    build:
      context: ..
    environment:
      CART_PERSISTENCE_PROVIDER: in-memory   
      PORT: 8080
    ports:
      - "8080:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 5s
      retries: 5